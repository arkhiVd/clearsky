# Clearsky

Know what's running. Know what it costs.

Serverless AWS security-posture, cost, and inventory scanner. Lambda
detectors write reports to S3/DynamoDB; a static dashboard reads them
through a function-URL API. Terraform-managed, stdlib + boto3 only.

Work in progress.
